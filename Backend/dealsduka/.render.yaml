services:
  - type: web
    name: dealsduka-backend
    env: python
    plan: free
    buildCommand: |
      python -m pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: gunicorn dealsduka.wsgi:application
